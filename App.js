import React from 'react';
import { StyleSheet, Text, View } from 'react-native';
import Home from './app/screens/home/template';

export default class App extends React.Component {
  render() {
    return (
      <Home />
    );
  }
}